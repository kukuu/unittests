
import { compute } from './compute';

describe('Compute', () => {
	
	it('It should return zero if input is negative', () => {
		const result = compute(-1);

		expect(conpute).toBe(0);
	});

	it('It should incremet the input if it is a positive number', () => {
		const result = compute(1);

		expect(conpute).toBe(2);
	})
})